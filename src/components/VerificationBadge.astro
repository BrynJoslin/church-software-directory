---
import { verificationLabel } from "../utils/content";

interface Props {
  status: "verified" | "partially-verified" | "needs-review";
}

const { status } = Astro.props;
---

<span class:list={["badge", `badge--${status}`]}>
  {verificationLabel(status)}
</span>
